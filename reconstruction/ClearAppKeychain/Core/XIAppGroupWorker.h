#import <Foundation/Foundation.h>

NS_ASSUME_NONNULL_BEGIN

/// Behavior-equivalent reconstruction of +[p2TmIsab a2RAEIEJ:].
/// The original argument is the app identity/bundle-ID token, not a caller-
/// supplied group dictionary. It resolves the r9itBRkI model through m4GOymjy:,
/// reads model.t2CTYndV, then enumerates that map's keys.
FOUNDATION_EXPORT void XIClearAppGroupContentsForBundleIdentifier(NSString *bundleIdentifier);

NS_ASSUME_NONNULL_END
