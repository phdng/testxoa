#import <Foundation/Foundation.h>

NS_ASSUME_NONNULL_BEGIN

/// Source-level reconstruction of runtime-installed +[p2TmIsab m4GOymjy:].
/// The original filters UIApplication.delegate.DanhSachAppDelegate with the
/// byte-exact predicate `SELF.bundleID LIKE[cd] %@` and returns firstObject.
/// A miss returns nil naturally.
FOUNDATION_EXPORT id _Nullable XIAppModelForBundleIdentifier(NSString *bundleIdentifier);

NS_ASSUME_NONNULL_END
